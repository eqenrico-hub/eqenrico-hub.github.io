I'm redesigning a section of a VST plugin (a scale-locked vocoder).

The attached image is ONLY for context — it shows the current layout so you know what elements exist and how they're labeled. Do NOT treat it as a starting point to tweak. Your output should be a completely fresh design based on your UI/UX judgment. Feel free to totally rearrange, restyle, reorient everything — as long as you use only the listed elements. The current layout is not a constraint, it's just an inventory.

CURRENT ELEMENTS (numbered to match badges in the reference image):
{{ELEMENT_LIST}}

SECTION SIZE: {{WIDTH}}mm × {{HEIGHT}}mm (W × H, matches image aspect).

TASK:
Redesign this section to be as compact, clean, minimal, and clear as possible. Maximum usability, minimum visual clutter.

STRICT CONSTRAINTS (do NOT violate):
- Use ONLY the elements listed above. Do not add "Variation", "Pan", "Output", "Edit", "Share", or any control not in the list.
- Do not duplicate elements. Each element appears exactly once.
- You MAY reposition, resize, restyle. You may shrink TUNING to an icon. You may convert sliders vertical ↔ horizontal.
- You MAY remove an element if you think it's unnecessary (mention in reasoning).
- You may NOT invent new widgets.

OUTPUT — PRODUCE ALL THREE:

=== REQUIRED 1: Element coordinate table ===
Return the same FORMAT shown below, but with YOUR OWN LAYOUT — do NOT copy these example numbers, they only illustrate the column structure.

FORMAT ONLY (do not reuse these coordinates — invent your own):
```
#  | Name      | Type     | X  | Y  | W  | H  | Notes
N  | <LABEL>   | <kind>   | ?  | ?  | ?  | ?  | <optional>
```

Rules:
- Coordinates in millimeters
- Origin (0,0) = top-left of the section, +X right, +Y down
- W + X must stay ≤ section width ; H + Y must stay ≤ section height
- Design the layout yourself — use your judgment as a UI designer. The table just captures your design mathematically so I can rebuild it in Blender.
- If you REMOVE an element: omit its row
- Keep original numbering (don't renumber); if renaming, keep the # the same

=== REQUIRED 2: Schematic image ===
Generate a clean flat-2D top-down mockup of your redesign:
- Aspect ratio as close to the input as your model allows (do not produce a square if you can avoid it; if you must, center the design in a wide strip)
- Dark chassis (#0a0a0a) with brass accents (#c9a86b), rounded corners
- Numbered yellow circular badges on each element matching your table's #
- No 3D, no perspective, no lighting effects — flat UI schematic only
- If your image tool cannot be invoked, say explicitly "image generation unavailable" and skip, do NOT promise one later

=== REQUIRED 3: Reasoning bullets ===
3–5 short bullets: what moved, what you grouped, what you removed (if anything), why.

=== OPTIONAL 4: ASCII layout ===
A quick ASCII sanity check (60 chars wide max):
```
+----------------------------------+
| [KEY] [SCALE TYPE]         [ S ] |
| [DEG] [TYPE]               [ S ] |
| [        T U N I N G        ⚙]   |
+----------------------------------+
```

AESTHETIC REFERENCE: dark chassis + thin brass frames (#c9a86b) + rounded corners + minimal typography. Keep this unless you have a strong reason to change.
