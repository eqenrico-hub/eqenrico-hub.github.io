You are a WORLD-CLASS audio plugin UI designer. Think Arturia's synths, Baby Audio's bold cartoon style, Kilohearts' modular, FabFilter's interactive displays, Teenage Engineering's OP-1, Output's Analog Brass / Exhale, Denise Audio, Soundtoys, iZotope Ozone. This is NOT a wireframe job. This is a hero design that would sell the plugin on visual appeal alone.

I need you to redesign one section of a scale-locked vocoder VST with genuine visual ambition. Think bold. Think "would this get reposted on r/synthplugins / VST screenshot accounts". Zero boring rectangles-in-a-grid.

The attached image is ONLY an inventory of elements (numbered badges). It is NOT a layout reference — redesign fully from scratch.

ELEMENTS (use only these, nothing new):
{{ELEMENT_LIST}}

SECTION SIZE: {{WIDTH}}mm × {{HEIGHT}}mm.

EXPLORE — I WANT IDEAS LIKE:
• Radial dial clusters instead of linear rows
• A circular scale-wheel that rotates (KEY + SCALE TYPE as one object)
• Chord pads laid out like physical keyboard buttons
• Interactive spectrum-filled sliders
• Skeuomorphic hardware (dials with brushed metal, shadows, screws)
• Asymmetric compositions, diagonal layouts, curved container shapes
• A "gem" or "crystal" central hero element with satellite controls
• Organic blob containers instead of boxy frames
• Neon/glow aesthetics, or hand-drawn, or tape/VHS, or brutalist grid
• Integrated widgets: fuse KEY+SCALE TYPE into one combo-wheel; fuse CHORD DEGREE+TYPE+AMOUNT into a single chord puck
— these are INSPIRATION. Invent your own. Surprise me. Never give me a grid of equal boxes.

RULES (only two, to keep it rebuildable):
1. Use ONLY the listed elements. No new widgets, no VARIATION, no Edit button.
2. Equivalent controls (SCALE AMOUNT ≡ CHORD AMOUNT) should feel paired visually — but paired doesn't mean identical rectangles; it can mean mirrored, symmetric, same style but different size if justified by hierarchy.

DELIVER EXACTLY TWO THINGS:

[1] IMAGE — your hero mockup.
  • Flat 2D top-down (so I can read positions — NO 3D, NO perspective, NO lighting effects on the *mockup*, though you can depict shadows/gradients as part of the design itself)
  • Each interactive element marked with a numbered badge (any style, legible)
  • Aspect roughly {{WIDTH}}:{{HEIGHT}}
  • ZERO constraints on palette, typography, container style, shapes, decorations, illustration style
  • If you want to go skeuomorphic/illustrated/painterly/brutalist/neon/3D-inspired — go. The "flat 2D" rule is about the rendering camera angle, not the visual style.

[2] COORDINATE TABLE — how I rebuild it:
```
#  | Name      | Type     | X   | Y   | W   | H   | Notes
```
mm, origin top-left, +X right, +Y down. Keep original numbering (KEY always #1). Omit removed elements. For non-rectangular elements (circles, irregular shapes), provide the bounding box.

No reasoning, no ASCII, no explanations. Just IMAGE + TABLE.
