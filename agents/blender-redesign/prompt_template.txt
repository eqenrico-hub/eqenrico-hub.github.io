I'm designing a VST plugin (scale-locked vocoder). I need you to act as a senior UI/UX designer — think Ableton, Teenage Engineering, Output, FabFilter — and propose a FRESH, OPINIONATED redesign of one section.

The attached image is ONLY an inventory of existing elements (shown with numbered yellow badges). It is NOT a starting point. Do not preserve any part of its layout, spacing, orientation, or visual logic unless you have a specific reason. Treat it as: "these are the controls that must exist; now redesign from scratch".

ELEMENTS THAT MUST APPEAR (use ONLY these — no new widgets):
{{ELEMENT_LIST}}

SECTION SIZE: {{WIDTH}}mm × {{HEIGHT}}mm.

DESIGN BRIEF:
Push for a layout a pro designer at Ableton or FabFilter would ship. Bold typography choices, unexpected groupings, novel control combinations (e.g. fuse dropdown+slider into one widget, use dials instead of sliders, replace buttons with toggles, stack vertically instead of horizontally, use a radial cluster, integrate TUNING as a pop-out drawer instead of a bar — invent). The current layout is mediocre; your job is to make it feel premium and original while staying intuitive.

Aesthetic reference: dark chassis (#0a0a0a) + thin brass accents (#c9a86b). Minimal text. You may change the aesthetic if you have a strong reason.

DELIVERABLES (only these two, nothing else):

=== 1. Schematic image ===
Generate a flat 2D top-down mockup of YOUR redesign.
- Aspect ratio as close to {{WIDTH}}:{{HEIGHT}} as your model allows
- Each interactive element marked with a numbered yellow circular badge, # matching the table below
- No 3D, no perspective, no lighting

=== 2. Coordinate table ===
```
#  | Name      | Type     | X  | Y  | W  | H
N  | <LABEL>   | <kind>   | ?  | ?  | ?  | ?
```
- All values in millimeters
- Origin (0,0) = top-left, +X right, +Y down
- Design the numbers based on YOUR mockup — don't copy any reference values
- If you remove an element: omit its row
- Keep the original numbering (e.g. KEY is always #1)

DO NOT include reasoning, ASCII diagrams, or apologies. Just the image + the table.
